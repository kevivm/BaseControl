using System;

namespace BaseControl
{
    class Print
    {
        public static void printResultDataParsed(string mostPopupatedCity, string longestName)
        {
            Console.WriteLine("Most populated: " + mostPopupatedCity);
            Console.WriteLine("Longest name: " + longestName);
        }
    }
}
